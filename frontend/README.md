# 栖物志 Item Habitat 前端

这是栖物志 Item Habitat 的 Vue 3 前端。

## 启动

先启动根目录后端：

```powershell
uv run backend/main.py
```

再启动前端：

```powershell
npm install
npm run dev
```

## 构建

```powershell
npm run build
```

## API

开发环境下，`/api` 会代理到 `http://127.0.0.1:8000`
