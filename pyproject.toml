[project]
name = "warehouse-system"
version = "0.1.0"
description = "Warehouse management system backend"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "fastapi>=0.115.0",
    "httpx>=0.27.0",
    "python-multipart>=0.0.9",
    "uvicorn>=0.30.0",
]
